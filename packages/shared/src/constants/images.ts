/** File extensions accepted for listing images. */
export const ALLOWED_IMAGE_EXTENSIONS = new Set(['.jpg', '.jpeg', '.png', '.webp']);

/** MIME types accepted for listing images. */
export const ALLOWED_IMAGE_TYPES = new Set(['image/jpeg', 'image/png', 'image/webp']);

/** Maximum number of images a listing can contain. */
export const MAX_LISTING_IMAGES = 3;
